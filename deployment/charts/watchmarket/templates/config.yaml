{{- if .Values.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: config
  namespace: {{ .Release.Namespace }}
data:
  {{- range $k, $v := .Values.configs }}
  {{- range $kk, $vv := $v }}
    {{ $kk }}: {{ $vv | quote }}
  {{- end }}
  {{- end }}
{{- end }}